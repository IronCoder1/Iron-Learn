+ accessing variables in functions like this:

11> NSString *captain = @"Kathryn Janeway";
12> NSLog(captain);


+ classes, creating objects, their methods, why and when to split them in header and implementation files.